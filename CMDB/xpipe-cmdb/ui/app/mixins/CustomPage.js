/**
 * @file CMDBuildUI.mixins.CustomPage
 * @mixin CMDBuildUI.mixins.CustomPage
 * @author Tecnoteca srl 
 * @access public
 * 
 * @classdesc
 * This mixin must be used in the main view of a custom page.
 */
Ext.define('CMDBuildUI.mixins.CustomPage', {
    mixinId: 'custompage-mixin'
});